{
  programs.uv = {
    homeManager = {
      programs = {
        ruff = {
          enable = true;
        };
        ty = {
          enable = true;
        };
        uv = {
          enable = true;
        };
      };
    };
  };
}
